{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        

        {
            "name": "Python Delta Node", // run: delta_node_start run, entry_points={"console_scripts": ["delta_node_start=delta_node.main:main"]}
            "type": "python",
            "request": "launch",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/delta_node/main.py",
            "args": ["run"],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}